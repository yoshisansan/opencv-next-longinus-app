import { useTranslation } from 'next-i18next';

const PageTitleH1 = ({target}: {target: string | JSX.Element | JSX.Element[]}) => {
  const { t } = useTranslation('common');

  return (
    <></>
  )
};

export default PageTitleH1